# Markdownlint configuration for Bum Scrolling (Starlight Docs)
MD013: false # Disable line length check for markdown docs with tables/code
MD033: false # Allow inline HTML elements (used in Starlight alerts/components)
MD024: false # Allow multiple headings with same content (e.g. in troubleshooting sections)
MD041: false # First line does not need to be top-level heading (frontmatter is used)
